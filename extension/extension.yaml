name: custom:simple-webhook
version: 0.0.10
minDynatraceVersion: "1.285"
author:
  name: "Dynatrace"

python:
  runtime:
    module: simple_webhook
    version:
      min: "3.10"

  activation:
    remote:
      path: activationSchema.json
