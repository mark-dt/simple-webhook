name: custom:simple-webhook
version: 0.0.10
minDynatraceVersion: "1.335"
author:
  name: "Dynatrace"

python:
  runtime:
    module: simple_webhook
    version:
      min: "3.14"

  activation:
    remote:
      path: activationSchema.json
